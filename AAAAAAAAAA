Error from server (BadRequest): error when creating "../../otel-ingress.yaml": Ingress in version "v1" cannot be handled as a Ingress: json: cannot unmarshal object into Go struct field IngressSpec.spec.tls of type []v1.IngressTLS

# gRPC Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: otel-collector-grpc-ingress
  namespace: monitoring
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/backend-protocol: "GRPC"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  ingressClassName: nginx
  tls:
    - dashboard
  rules:
  - host: dashboard
    http:
      paths:
      - path: /otel
        pathType: Prefix
        backend:
          service:
            name: otel-collector-collector
            port:
              number: 4317

---
# HTTP Ingress
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: otel-collector-http-ingress
  namespace: monitoring
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  tls:
    - dashboard
  rules:
  - host: dashboard
    http:
      paths:
      - path: /otel
        pathType: Prefix
        backend:
          service:
            name: otel-collector-collector
            port:
              number: 4318  # OTLP HTTP port

