Error: execution error at (loki/templates/validate.yaml:19:4): Cannot run scalable targets (backend, read, write) or distributed targets without an object storage backend.
Release "loki" does not exist. Installing it now.
##[error]Error: execution error at (loki/templates/validate.yaml:19:4): Cannot run scalable targets (backend, read, write) or distributed targets without an object storage backend.




deploymentMode: SimpleScalable

# WRITE COMPONENT
write:
  enabled: true
  replicas: 2
  persistence:
    enabled: true
    storageClass: standard # <-- change if you use a different StorageClass
    accessModes:
      - ReadWriteOnce
    size: 10Gi
  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 1Gi

# READ COMPONENT
read:
  enabled: true
  replicas: 2
  resources:
    requests:
      cpu: 200m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi

# BACKEND COMPONENT
backend:
  enabled: true
  replicas: 1
  persistence:
    enabled: true
    storageClass: standard
    accessModes:
      - ReadWriteOnce
    size: 10Gi
  resources:
    requests:
      cpu: 200m
      memory: 512Mi
    limits:
      cpu: 500m
      memory: 1Gi

# GATEWAY (frontend reverse proxy)
gateway:
  enabled: true
  replicas: 1
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: 200m
      memory: 256Mi

# LOKI CONFIGURATION
loki:
  auth_enabled: false

  commonConfig:
    replication_factor: 2

  storage:
    type: filesystem
    filesystem:
      directory: /var/loki/chunks

  schemaConfig:
    configs:
      - from: "2024-01-01"
        store: tsdb
        object_store: filesystem
        schema: v13
        index:
          prefix: index_
          period: 24h

  compactor:
    working_directory: /var/loki/compactor
    shared_store: filesystem

  limits_config:
    retention_period: 7d
    reject_old_samples: true
    reject_old_samples_max_age: 168h
    max_query_lookback: 24h

  chunk_store_config:
    max_look_back_period: 24h

# SERVICE SETTINGS
service:
  type: ClusterIP
  port: 3100
