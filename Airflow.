 app.kubernetes.io/instance=grafana,app.kubernetes.io/name=grafana,app.kubernetes.io/version=12.0.1,helm.sh/chart=grafana-9.2.3,pod-template-hash=66fd694cc9

# Service Configuration
service:
  enabled: true
  type: ClusterIP
  port: 80
  targetPort: 3000
  portName: grafana-http
  selector:
    app.kubernetes.io/name: grafana  # Standard k8s label
    app.kubernetes.io/instance: grafana

# Pod Labels (Added this section)
podLabels:
  app.kubernetes.io/name: grafana
  app.kubernetes.io/instance: grafana
  app.kubernetes.io/version: "9.5.3"  # Match your Grafana version
  app: grafana                        # Backward compatibility

# Deployment Template (Ensure this exists)
deployment:
  spec:
    template:
      metadata:
        labels:
          app.kubernetes.io/name: grafana
          app.kubernetes.io/instance: grafana
