We have successfully set up a new database server and are in the process of migrating all data to it.

Current Progress:

Established a connection to the new DB server.

Successfully backed up the current database.

Imported the database into the new server.

Current Challenge:
Ensuring the application correctly points to the new DB server.

Request:
Could you kindly provide steps to:

Redirect the application to the new DB server.

Validate the connection to confirm everything is functioning as expected.

Affected Products:

Nprinting

Sense

Your assistance would be greatly appreciated.




RUN apk update && apk add --no-cache \
bash \
gcc \
musl-dev \
postgresql-dev \
libffi-dev \
openssl-dev \
build-base \
python3-dev \
py3-pip \
abseil-cpp-dev \
re2
   

# Install Python packages
RUN pip install --no-cache-dir \
psycopg2-binary \
pytz \
pybind11 \
apache-airflow

