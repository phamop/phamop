The DR activities was successful will all the critical component working as expected and validated by the Data Analyst 
However, we encountered an unexpected License issue with Nprinting  post-recovery.
Action: A vendor case has been opened (Case #000377137) with full details, 





RUN apk update && apk add --no-cache \
bash \
gcc \
musl-dev \
postgresql-dev \
libffi-dev \
openssl-dev \
build-base \
python3-dev \
py3-pip \
abseil-cpp-dev \
re2
   

# Install Python packages
RUN pip install --no-cache-dir \
psycopg2-binary \
pytz \
pybind11 \
apache-airflow

