 will contact James to discuss the additional areas that need to be included in the validation checklist. Once confirmed, I will ensure the validation is performed and the results are shared before proceeding with the upgrade.


RUN apk update && apk add --no-cache \
bash \
gcc \
musl-dev \
postgresql-dev \
libffi-dev \
openssl-dev \
build-base \
python3-dev \
py3-pip \
abseil-cpp-dev \
re2
   

# Install Python packages
RUN pip install --no-cache-dir \
psycopg2-binary \
pytz \
pybind11 \
apache-airflow

