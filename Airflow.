string to search for "opentelemetry-helm-charts using azure app insights as backend"

https://blog.arkahna.io/integrating-opentelemetry-with-azure-application-insights
FROM docker.io/python:3.12.10-alpine3.21

Error: UPGRADE FAILED: failed to create resource: clusterroles.rbac.authorization.k8s.io "my-opentelemetry-collector" is forbidden: user 

create your own ClusterRole and ClusterRoleBinding, and associate the ClusterRole to the ServiceAccount via ClusterRoleBinding otel-collector

clusterroles.rbac.authorization.k8s.io "my-opentelemetry-collector" is forbidden: user "SP" (groups=["system:authenticated"]) is attempting to grant RBAC permissions not currently held:
{APIGroups:[""], Resources:["nodes/stats"], Verbs:["get" "watch" "list"]}
	* clusterroles.rbac.authorization.k8s.io "my-opentelemetry-collector" not found


RUN apk update && apk add --no-cache \
bash \
gcc \
musl-dev \
postgresql-dev \
libffi-dev \
openssl-dev \
build-base \
python3-dev \
py3-pip \
abseil-cpp-dev \
re2
   

# Install Python packages
RUN pip install --no-cache-dir \
psycopg2-binary \
pytz \
pybind11 \
apache-airflow

