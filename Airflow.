 The provided article link does not clarify where DB connection can be modified so that the new DB can have a successful connection to the Qlik (Nprinting and Sense) application. I already mentioned in my previous comment that we were able to backup DB successfully.  
Please can you clarify the location on the window server where the DB connection needs to be defned for the 2 products

RUN apk update && apk add --no-cache \
bash \
gcc \
musl-dev \
postgresql-dev \
libffi-dev \
openssl-dev \
build-base \
python3-dev \
py3-pip \
abseil-cpp-dev \
re2
   

# Install Python packages
RUN pip install --no-cache-dir \
psycopg2-binary \
pytz \
pybind11 \
apache-airflow

