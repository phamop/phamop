We are created a new DB server, and we would like to migrate all the data to the new DB server. 
we have been able to be established connection to the need DB server. However, to ensure the application is pointing to the DB server is the challenge we are facing. 
We are able to successfully backup our current DB and has import the DB into the new Server.

So if you can kindly provide step to ensure we are pointing to the new server DB server and validation. 
We have 2 products
Nprinting 
Sense.


RUN apk update && apk add --no-cache \
bash \
gcc \
musl-dev \
postgresql-dev \
libffi-dev \
openssl-dev \
build-base \
python3-dev \
py3-pip \
abseil-cpp-dev \
re2
   

# Install Python packages
RUN pip install --no-cache-dir \
psycopg2-binary \
pytz \
pybind11 \
apache-airflow

